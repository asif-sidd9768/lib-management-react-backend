<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Read & Grow</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link" href="/quotes">Quotes</a>
                <% if(currentUser){ %> 
                    <a class="nav-link" href="/goals">Goals</a>
                    <a class="nav-link" href="/book/not-available">Borrowed-Books</a>
                <% } %> 
            </div>
            <div class="mx-3 navbar-nav ms-auto">
                <% if(!currentUser){ %>
                    <a class="nav-link" href="/login">Login</a>
                    <a class="nav-link" href="/register">Register</a>
                    <% } else{%>
                    <a href="/profile/<%=currentUser.username%> " class="nav-link me-3 text-warning"><%=currentUser.username.toUpperCase()%></a>
                    <a class="nav-link" href="/logout">Logout</a>
                    <% } %>
            </div>
        </div>
    </div>
</nav>